<h1>ResultatExamen</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Libelle</th>
            <td>{{ entity.libelle }}</td>
        </tr>
        <tr>
            <th>Admisfeminin</th>
            <td>{{ entity.admisFeminin }}</td>
        </tr>
        <tr>
            <th>Admismasculin</th>
            <td>{{ entity.admisMasculin }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('resultatexamen') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('resultatexamen_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('resultatexamen_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>

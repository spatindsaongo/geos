<h1>ResultatExamen list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Libelle</th>
            <th>Admisfeminin</th>
            <th>Admismasculin</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('resultatexamen_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.libelle }}</td>
            <td>{{ entity.admisFeminin }}</td>
            <td>{{ entity.admisMasculin }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('resultatexamen_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('resultatexamen_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('resultatexamen_new') }}">
            Create a new entry
        </a>
    </li>
</ul>

<h1>Classe list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Libelle</th>
            <th>Niveau</th>
            <th>Effectiffeminin</th>
            <th>Effectifmasculin</th>
            <th>Anneescolaire</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('classe_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.libelle }}</td>
            <td>{{ entity.niveau }}</td>
            <td>{{ entity.effectifFeminin }}</td>
            <td>{{ entity.effectifMasculin }}</td>
            <td>{{ entity.anneeScolaire }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('classe_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('classe_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('classe_new') }}">
            Create a new entry
        </a>
    </li>
</ul>

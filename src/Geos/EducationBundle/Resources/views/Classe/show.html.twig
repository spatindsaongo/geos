<h1>Classe</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Libelle</th>
            <td>{{ entity.libelle }}</td>
        </tr>
        <tr>
            <th>Niveau</th>
            <td>{{ entity.niveau }}</td>
        </tr>
        <tr>
            <th>Effectiffeminin</th>
            <td>{{ entity.effectifFeminin }}</td>
        </tr>
        <tr>
            <th>Effectifmasculin</th>
            <td>{{ entity.effectifMasculin }}</td>
        </tr>
        <tr>
            <th>Anneescolaire</th>
            <td>{{ entity.anneeScolaire }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('classe') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('classe_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('classe_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>

{% extends 'GeosMainBundle::entityManagement.html.twig' %}
{% block entityManagement %}
<h2 class="page-header" >Lot : {{ pagination.totalCount }} élements dans la liste
	<div style="float: right;" class="btn-group">
	<a class="btn btn-primary" href="{{ path('lot_new') }}">
		<i class="icon-plus-sign icon-white"></i>
		ajouter
	</a>
	<a class="btn" href="#">
    	<i class=" icon-print"></i>
        imprimer
    </a>
    </div>
</h2>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Id</th>
            <th>{{ pagination.sortable('Numero', 'lot.numero')|raw }}</th>
            <th>Section</th>
            <th>Commune</th>
            <th>Province</th>
            <th>Region</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in pagination %}
        <tr>
            <td><a href="{{ path('lot_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.numero }}</td>
            <td>{{ entity.section }}</td>
            <td>{{ entity.section.commune }}</td>
            <td>{{ entity.section.commune.province }}</td>
            <td>{{ entity.section.commune.province.region }}</td>
            <td>
            	<p>
	                <a href="{{ path('lot_show', { 'id': entity.id }) }}" title="Voir">
	                <i class="icon-eye-open"></i>
	                </a>
	                <a href="{{ path('lot_edit', { 'id': entity.id }) }}" title="Modifier">
	                <i class="icon-edit"></i>
	                </a>
	               	<a href="{{ path('lot_delete', { 'id': entity.id }) }}" title="Supprimer">
	                <i class="icon-trash"></i>
	                </a>
                </p>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{{ pagination.render()|raw }}
{% endblock %}